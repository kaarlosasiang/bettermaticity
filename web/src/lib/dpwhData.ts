/** Philippine peso amounts from procurement notices (not millions). */
export function pesoFull(amount: number): string {
  return `₱${amount.toLocaleString('en-PH', { minimumFractionDigits: 2, maximumFractionDigits: 2 })}`;
}
