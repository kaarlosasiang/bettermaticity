name: Lighthouse CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lighthouse:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Start server
        # serve.py emulates the .htaccess clean-URL rewrites (plain http.server
        # cannot), so extensionless React routes like /services/health resolve.
        run: |
          python3 serve.py -d dist -p 8080 &
          sleep 3

      - name: Run Lighthouse CI
        uses: treosh/lighthouse-ci-action@v12
        with:
          urls: |
            http://localhost:8080/
            http://localhost:8080/services/
            http://localhost:8080/services/health
            http://localhost:8080/contact/
            http://localhost:8080/government/
          configPath: .lighthouserc.json
          uploadArtifacts: true
